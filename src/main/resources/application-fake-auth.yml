# Profil « fake-auth » — Auth simulée en mémoire (sans Kernel)
# Démarrage : SPRING_PROFILES_ACTIVE=local,demo,fake-auth
# COMPTES DISPONIBLES (mot de passe : FleetMan2026!) :
#   superadmin@fleetman.cm  → FLEET_SUPER_ADMIN
#   admin@fleetman.cm       → FLEET_ADMIN
#   manager1@fleetman.cm    → FLEET_MANAGER  (Jean Dupont)
#   manager2@fleetman.cm    → FLEET_MANAGER  (Paul Foka)
#   driver1@fleetman.cm     → FLEET_DRIVER   (Thomas Tabi)

application:
  auth:
    mode: fake
  external:
    geofence-mode: fake
    vehicle-mode: fake
    payment-mode: fake

